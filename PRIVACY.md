# Privacy

Untitled Exact Search processes the current search text and the titles of results already rendered on the page. Processing happens entirely in the browser tab.

The extension:

- does not transmit data;
- does not store search terms or track information;
- does not use analytics;
- does not request access outside `https://untitled.stream/library/project/*`.
