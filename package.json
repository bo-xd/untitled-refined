{
  "name": "untitled-exact-search",
  "version": "0.2.0",
  "private": true,
  "description": "A permission-minimal browser extension that fixes project-track search ranking on untitled.stream.",
  "scripts": {
    "check": "node --check src/ranking.js && node --check src/content.js",
    "test": "node --test"
  }
}
